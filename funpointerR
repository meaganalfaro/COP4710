#include <stdio.h>
#include <stdlib.h>

int* add(int* x, int* y){
    int* z = (int*) malloc(sizeof(int));
    *z = *x + *y;
    return z;
}
int main() {
    
    int a = 5;
    int b = 7;
    int* c;
    c = add(&a,&b);
    
    printf("%d\n", *c);
    
    return 0;
}
